services:
  - type: web
    name: bg-remover-backend
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && node server.js
    envVars:
      - key: NODE_ENV
        value: production
      
      - key: FRONTEND_URL
        value: https://bg-remover.vercel.app  # Your real frontend URL
    autoDeploy: true  #